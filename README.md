`hello` is a `c++` "Hello World" app
---
`main.cpp` can be compiled using `g++` and the `-o` flag to override the default `a.out` output executable   
run `hello` via `./hello` from within its containing directory  

